source "http://rubygems.org"

# Specify your gem's dependencies in vagrant-rake.gemspec
gem "vagrant-hosts", :path => "."

# Want to use released versions for now
# gem "vagrant", :git => "git://github.com/mitchellh/vagrant.git"
# gem "virtualbox"

group :test do
  gem "rspec", "~> 2.6.0"
  gem "rake"
  gem "ruby-debug"
end
